# Contributing to Capyra

感谢你改进 Capyra。提交代码前，请先确认改动的用户场景、权限边界和失败行为。

## 开发环境

需要 Node.js 22.16 或更新版本：

```sh
git clone https://github.com/dxeledx/capyra.git
cd capyra
npm ci
npm run build
```

## 工作方式

1. 从 `main` 创建一个范围清楚的分支。
2. 保持改动集中，不混入本机配置、日志、构建产物或格式化噪声。
3. 修改安全边界、协议、存储格式或插件契约时，同步更新相关文档。
4. 为有行为分支的功能补充测试；不要依赖真实用户目录、账号或公网凭据。
5. 提交前运行适合改动范围的检查。

```sh
npm run build
npm test
```

完整检查：

```sh
npm run check
```

## 代码约定

- 使用 TypeScript 和现有模块结构。
- 在数据流、安全约束和不直观分支附近写清楚“为什么”的注释。
- 错误信息应告诉用户发生了什么以及下一步怎么做，避免返回密钥、绝对私有路径或内部堆栈。
- 新能力优先通过插件契约实现；内核只保留生命周期、权限、任务和服务组合所需的稳定机制。
- 新依赖需要说明用途、体积和运行时成本。重型提供者应按需加载。

## Pull Request

PR 描述应包括触发问题或使用场景、最终行为、验证方式，以及权限、兼容性或迁移影响。

请勿提交真实 `.env`、令牌、私钥、OAuth 数据、设备地址、用户文件或 `.capyra/` 状态。
